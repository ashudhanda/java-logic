# Largest Of Two

Goal: find the larger value between two numbers.

Steps:
1. Read `a` and `b`.
2. Compare both values.
3. Print the value that is greater.

Java idea:

```java
int largest = a > b ? a : b;
System.out.println(largest);
```
