# Multiplication Table

Goal: print the table of a number from `1` to `10`.

Steps:
1. Read `n`.
2. Loop from `1` to `10`.
3. Multiply `n` by the loop counter.

Java idea:

```java
for (int i = 1; i <= 10; i++) {
    System.out.println(n + " x " + i + " = " + (n * i));
}
```
