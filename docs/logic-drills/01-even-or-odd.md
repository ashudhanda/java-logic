# Even Or Odd

Goal: decide whether a number is even or odd.

Steps:
1. Read an integer `n`.
2. Check `n % 2`.
3. Print `Even` when the remainder is `0`; otherwise print `Odd`.

Java idea:

```java
if (n % 2 == 0) {
    System.out.println("Even");
} else {
    System.out.println("Odd");
}
```
