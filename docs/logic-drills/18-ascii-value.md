# ASCII Value

Goal: print the numeric value of a character.

Steps:
1. Read a character.
2. Convert it to `int`.
3. Print the result.

Java idea:

```java
char ch = 'A';
int value = ch;
System.out.println(value);
```
